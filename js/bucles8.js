let num = parseInt(prompt("Ingrese un número"))

for (let i = 0; i <= num; i++) {
    for (let repetir = 1; repetir <= i; repetir++) {
        document.write(repetir)

    }
    document.write("<br>")
}